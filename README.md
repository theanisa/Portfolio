# Portfolio - Anisa Binte Faruk

Personal portfolio scaffolded with Vite + React.

Run locally:

```bash
npm install
npm run dev
```

This project uses plain CSS, React functional components, and beginner-friendly structure. Edit content in `src/components` and `src/data`.
